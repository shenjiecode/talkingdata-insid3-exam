## 人工审核结论

结论：不正确

### 核心理由

题目要求处理已经给出的低语义特征：逐 patch 做 L2 归一化，按固定顺序合并，再用全部 patch 的通道均值做中心化。提交代码实际做的是根据网格坐标生成一组人工位置特征，再用随机正交矩阵把它们映射到指定通道数。返回值虽然也是 `(P, C)`，但它没有保留或处理题目给出的特征内容，后续 SVD 得到的也会是人工构造的位置变化。

因此，问题出在所实现的方法本身。即使补一个接收特征图的参数，这段计算仍然没有完成要求。

### 代码依据

1. **探针来自坐标函数和随机映射。** `patch_grid_coordinates` 生成 `y, x`，`channels` 随后堆叠坐标、多项式和三角函数。`a` 是按种子生成的随机矩阵，QR 分解得到 `q`，最后由 `probe = z @ q.T` 生成输出。整条计算都没有读取给定的低语义特征。固定网格与随机种子后，即使换一份内容完全不同的特征输入，这套构造也无法反映这种变化。

2. **按列标准化不能代替逐 patch 的 L2 归一化。** `z - z.mean(dim=0)` 和 `z / z.std(dim=0)` 处理的是人工通道在不同位置上的均值和标准差。题目要处理的是每个 patch 沿通道方向的 L2 范数。`q` 的列正交只保证映射保留 `z` 各行的范数，不会把这些范数变成 1；代码中没有对给定 patch 执行 L2 归一化的步骤。

3. **中心化对象和顺序不符合要求。** 默认设置下，如果 `z` 的标准化结果有限，`z` 每列均值为零，线性映射后的 `probe` 也可以保持通道均值为零。这一点不能算成“完全没有中心化”。但中心化发生在人工构造的 `z` 上，缺少“给定特征逐 patch 归一化后，在全部图像的 patch 上统一中心化”的流程。关闭 `normalize_columns` 会跳过中心化；开启 `noise_scale` 后又会添加未重新中心化的噪声，也没有保证最终通道均值为零。

4. **缺少多图整理和来源元数据。** `meshgrid(indexing="ij")` 配合 `reshape(-1)` 确实给单个人工网格规定了顺序，但代码没有按输入图像顺序合并多个已有特征图，也没有返回各来源网格的形状或 patch 对应信息，只有一个 `probe` 张量。

题目还要求不修改输入；这段代码没有接收要处理的特征图，不能凭“没有修改任何特征输入”认定已经满足该流程。以上判断只依据题面与提交代码，没有运行、修复代码，也没有借用自动测试结果作为结论。
